@model List<CategoryVM>

@{
    ViewData["Title"] = "Categories";
}

<div class="container my-5">
    <h1 class="text-center">Archived Categories</h1>
    <div class="bg-light rounded h-100 p-4">
        <h6 class="mb-4">Basic Table</h6>
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Options</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <th>@item.Name</th>
                        <td>
                            <form method="post" asp-action="ExractCategory" asp-route-id="@item.Id" class="d-inline">
                                <button type="submit" class="btn btn-primary">Exract</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

